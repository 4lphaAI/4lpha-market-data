# Data-plane handoff: indicator revision 2 for the TradFi AI-trade score (BB, StochRSI, VWAP, gap, ORB, equity regime)

Date: 2026-09-21
Target repository: D:\4lphaDATA-marketplace
Status: **handoff for specification and build; nothing in this repository has been changed by the authoring session.** Operator rulings are recorded in the execution plane (`D:\4lpha-execution\.agents\memory\tradfi-ai-trade-rulings-2026-09-21.md`); the post-mortem that motivates this is `tradfi-live-run-diagnosis-2026-09-21.md` in the same directory.

Implemented (subsequent task, 2026-09-21): indicator revision 2 is built and verified offline — `npm run typecheck` clean, `node --test` **TESTS 658 / PASS 658 / FAIL 0 / SKIPPED 0** (12 new in `test/tradingFeatures.rev2.test.ts`, one rev 1 test updated for the two added watch-set pools). Not committed, not deployed; the execution plane's decoder still needs the `1 | 2` bump before it decodes rev 1 metrics off a rev 2 payload.

Implemented (§8 follow-up, 2026-09-22): coverage and cadence built and verified offline — `npm run typecheck` and `npm run build` clean, `node --test` **TESTS 659 / PASS 659 / FAIL 0 / SKIPPED 0**. Not committed, not deployed.

Implemented (§9 follow-up, 2026-09-22): admission built and verified offline — `npm run typecheck` and `npm run build` clean, `node --test` **TESTS 660 / PASS 660 / FAIL 0 / SKIPPED 0**. Not committed, not deployed.

- Root cause was not `DUE_PER_CYCLE` alone: `src/query/poolOhlcv.ts` had its own process-local in-flight concurrency cap (`r.inFlight.size < 4`), entirely separate from both `DUE_PER_CYCLE` and the `ohlcvTransport` budgets, and it was the dominant source of the measured `admission_limit` denials — most of a cycle's own admissions never reached the transport budget check at all. Renamed to `MAX_IN_FLIGHT_REFRESHES` and raised 4 → 22.
- `DUE_PER_CYCLE` (`src/jobs/tradingFeatures.ts`) raised 10 → 22. Every default-selection entry is `currency: "token"`, which tries `geckoterminal` then falls back to `dexpaprika` (`query/poolOhlcv.ts`'s `refresh`), so combined real capacity is `BUDGETS.geckoterminal (10) + BUDGETS.dexpaprika (12) = 22` — kept 1:1 with that combined number, not padded, same reasoning as §8.
- Separately, and probably the bigger fix: the producer's backoff was conflating "this attempt never reached the provider because our own admission/budget control said not-this-tick" with "the provider answered and something is actually wrong." Both got the same exponential backoff (up to 5 min) and the same `consecutiveFailures` escalation. At 10 pools that rarely mattered; at 36+ pools `admission_limit`/`budget_exhausted` is the *routine* outcome for most of a cycle, so denied candidates were being pushed further and further behind every pass instead of settling into a stable rotation — which is very likely why raising `DUE_PER_CYCLE` in §8 didn't clear the backlog on its own. `admission_limit`/`budget_exhausted` (plus the pre-existing `refresh_lease`) now retry flat at `now + 60_000` with no backoff escalation, and are counted as quiet (not a job failure) when a whole pass is nothing but self-throttling.
- With these three together, worked example for the 36-pool 15m burst (all 36 bars close on the same wall-clock boundary): cycle 1 admits the 22 oldest-due candidates — since 15m candidates are refreshed less often than 5m ones, their `attemptedAt` is systematically older, so the fairness sort favors them at the moment of a compound close — cycle 2 (~60s later, still inside the `close + step + 90s` grace) picks up the remaining 14. Full 15m coverage lands within roughly one cycle-pair, not several.
- **Caveat, stated rather than hidden:** this reasoning does not extend cleanly to the top of the hour, where 5m + 15m + 1h all close at once (108 candidates, 36 each). 1h candidates are refreshed even less often than 15m ones, so they sort *first* under the same oldest-`attemptedAt`-wins fairness rule, which can push 15m coverage behind schedule specifically at `:00` marks. Admission is still purely FIFO-by-age with no explicit interval-priority; fixing that (if `:00`-mark misses are observed live) is a distinct, larger change and was not built here — flagging it now rather than asserting the 90s SLA is met unconditionally.
- New test: `test/tradingFeatures.test.ts` ("handoff §9: self-throttle denials retry next tick without backoff and never fail the job") pins the no-backoff/no-escalation/quiet-not-failed behavior directly. Existing cap-fairness and in-flight-admission tests rescaled for the new constants (10→22, and the in-flight test's pool count 4→22).

- `MAX_POOLS` (`src/jobs/tradingFeatures.ts`) raised 10 → 40. `featureSelection`'s oversize-watchlist error message now names the live ceiling instead of a hardcoded "10".
- `defaultFeatureSelection` is now async and store-backed. New `defaultRwaFeatureSelection` reads `buildUniverse`'s `bstocks`/`ondo` lanes and, per token, keeps the deepest venue at or above the $10k depth floor (`SPREAD_MIN_LIQUIDITY_USD`, the same floor `spread-history` already watches at) — no hand-enumeration, so a new RWA admission is covered the next producer cycle. The two hardcoded NVDAB/TSLAB seeds are gone; the sweep now produces them. SPYB/QQQB stay pinned explicitly (not left to the sweep) so `/trading/regime/us-equity` keeps a known-good pool even on a cycle where the venue sweep hasn't run. If the union of majors + RWA sweep + equity legs ever exceeds `MAX_POOLS`, the lowest-liquidity RWA rows are trimmed with a warning log rather than throwing and starving every series.
- The read route's 10-pool batch bound (`GET /trading/features/v2?pools=...`, `src/server.ts`) was already independent of `MAX_POOLS` — confirmed, not touched.
- Cadence root cause was not the close+lag formula (`refreshAfter = closeTime + step + PUBLICATION_LAG_MS` was already correct) — it was `DUE_PER_CYCLE` (the job's per-60s-cycle admission count) colliding with the **hard, shared** `geckoterminal` transport budget in `src/adapters/ohlcvTransport.ts` (`BUDGETS`, atomic-leased across replicas, also serves `/pools/:address/ohlcv`). The old 4/cycle was already sized exactly to the old 8/minute Gecko budget (4 series × ≤2 HTTP). At the new pool count, steady-state warm demand is `~32*(1/5+1/15+1/60) ≈ 9.07` Gecko requests/minute — already past the old 8/minute budget before counting the RTH-close burst where every session-anchored series across all three intervals goes due at once. Raising `DUE_PER_CYCLE` alone without raising the transport budget would have made this worse: excess admissions get `budgetDenied`, which the job records as a failure and punishes with exponential backoff (up to 5 min). Both moved together: `BUDGETS.geckoterminal` 8 → 10, `DUE_PER_CYCLE` 4 → 10, kept 1:1 with the budget rather than padded. Neither number is a measured GeckoTerminal ceiling — none is documented in this repo — so this is "just enough for today's steady state," not a permanent answer; the RTH-close burst (all three intervals closing simultaneously across every session-anchored pool) still drains over several cycles, not one, and that is expected rather than fixed by this change.
- Tests updated for the new constants/behavior: `test/tradingFeatures.v2.test.ts` (`defaultFeatureSelection` is now async/store-backed; new test for deepest-venue-wins RWA sweep), `test/tradingFeatures.test.ts` (oversize-watchlist message, per-cycle cap fairness test rescaled to 7 pools/cap 10), `test/poolOhlcvFallback.test.ts` (shared-budget assertion 8 → 10).

Route / field additions:

- `parameters.indicatorRevision: 2`; `indicatorWarmupBars` + `{ bb20: 20, stochRsi14: 42, vwapSession: 1, gap: 1, orb: 6 | 2 | 0 }` (per interval); `bbPeriod`, `bbSigma`, `bbDeviation: "population"`, `stochRsiPeriods: [14, 14]`; `sessionClock: { timeZone: "America/New_York", rth: "09:30-16:00", close: "16:00-20:00", holidays: "none" }`.
- `metrics` + `bbMiddle20 bbUpper20 bbLower20 bbPosition20 bbWidthPct20 stochRsi14 lastRthClose gapPct vwapSession vwapDistancePct orbHigh orbLow orbBreakPct`. `lastRthClose` carries `asOf` (bucket close). New reasons: `zero_width zero_range not_us_equity no_rth_close_in_window zero_volume interval_too_coarse orb_not_formed not_rth`.
- `session: { usEquity, reason, state: rth|close|overnight|null, nextBoundaryAt, sessionStart, lastRthCloseAt, evaluatedAt }` on every rev 2 snapshot — `sessionState` lives here rather than as a `Metric`, whose `value` is numeric.
- `input.referenceSession: { underlyingTicker, marketStatus, openState, asOf } | null` retained for replay; surfaced read-side as `identity.referenceSession`. v1 payloads strip it.
- `/trading/features/v2/pools` → each pool carries `usEquity: boolean`.
- `GET /trading/regime/us-equity` (auth, envelope) → `{ asOf, sessionState, nextBoundaryAt, sessionClock, spy, qqq, regime, reasons, rule }`; `meta.staleness` `dead` when `unavailable`.
- Default watch set + SPYB/USDT 0.01% `0x7aa6d92fc369a8c1edc631a3aac44efb0808ddbf` and QQQB/USDT 0.01% `0xe531fcb1f5a195de7608b9f4f9518544c2cdb693` (8 of 10).

Deviations from §3, stated: bStocks report `marketStatus: null` (24/7), so the "US-equity `marketStatus`" qualifier is read as *platform `bstock`/`ondo` with an `underlyingTicker`* — the clock is the underlying's. The reference is read from `universe:rwa` at any staleness (the ticker never changes; `asOf` is carried). Measured on the live SPYB pool during Monday RTH: `gapPct` is unavailable on 5m always and on 15m Mondays (last RTH close ≥17.5 h back, window 10 h / 30 h), available on 1h (+0.98 % vs Friday's close); ORB formed on 15m (768.33 / 767.15, break +0.28 %).

## 1. Why

The first two-day live run of the TradFi v2 AI-trade agent (`tradfi-agent-01`, 100 USDT, 2026-09-19 → 21) closed 26 positions, 0 winners, −1.8 %. The execution-plane diagnosis: the LLM decided every exit on a bare prompt, and the indicators this plane already publishes (EMA12/26, ROC10, ATR14, RVOL20 and the `indicatorRevision: 1` additions RSI14, MACD 12/26/9, momentum10) were pasted into the prompt as JSON but drove **no rule**. The operator ruled that the execution plane adopts a deterministic 13-component score (Neural Alpha's shape: buy ≥ 14 / strong ≥ 38, falling-knife veto, LLM only nudges confidence) and that the missing components are produced **here first**, before the execution phase starts.

Components already served by v2 + rev 1: EMA12, EMA26, emaSpreadPct, ROC10, ATR14, ATR%, RVOL20, RSI14, MACD line, signal9, histogram, momentum10.
Missing, this handoff: **Bollinger 20/2σ, StochRSI 14/14, VWAP, gap vs last NYSE regular close, opening-range breakout (ORB), and an equity-market regime fact.**

## 2. Read first, preserve the architecture

`AGENTS.md`, `CLAUDE.md`, `README.md` ("indicatorRevision: 1" paragraph), `TRADING-FEATURES-SPEC.md`, `TRADING-FEATURES-V2.md`, `TRADFI-DATA-RESULT-2026-09-17.md`, `src/query/tradingFeatures.ts` (the `calculateFeatures` producer; rev 1 lives in the `version === FEATURE_VERSION_V2` branches around lines 160–225), `src/jobs/tradingFeatures.ts`, `src/adapters/binanceRwa.ts` (`openState` / `marketStatus`), `src/core/models.ts` (`referencePriceUsd`, `tokenToShareRatio`, `premiumBps`, `marketStatus`).

Invariants that stay: additive only (a new `parameters.indicatorRevision: 2` on the existing `pool-features-v2` payload; v1 and rev 1 numbers unchanged); each metric carries `available / reason / requiredBars / usableBars / unit`; null is never zero; no synthetic bars, no interpolation, no upsampling; the 120-bucket bounded window; the same one selected observation feeds every metric; replay via the stored `/input` snapshot must reproduce rev 2 exactly (extend the snapshot only with the extra inputs named below). Public BSC RPC posture unchanged; no paid provider.

## 3. What to add

### 3.1 Bar-only metrics (same series, no new inputs)

| Metric | Definition | Required bars | Unit |
|---|---|---:|---|
| `bbMiddle20` | SMA20 of close | 20 | price unit |
| `bbUpper20` / `bbLower20` | middle ± 2 × population σ of the same 20 closes | 20 | price unit |
| `bbPosition20` | (close − lower) / (upper − lower), clamp none; `reason: zero_width` when upper == lower | 20 | ratio |
| `bbWidthPct20` | (upper − lower) / middle × 100 | 20 | percent |
| `stochRsi14` | (RSI14 − min RSI14 over 14) / (max − min) over the last 14 RSI values; needs 14 RSI observations, each needing 29 bars → 42 bars; `reason: zero_range` when max == min | 42 | ratio 0–1 |

Reuse the rev 1 `rsi14` recurrence bar-for-bar (Wilder, SMA seed over 14 deltas, `50` when flat). Publish `stochRsi14` as the raw value; the execution plane smooths if it wants to. Warm-up rows go in `indicatorWarmupBars`.

### 3.2 Session-anchored metrics (need the NYSE clock and the RWA reference)

These exist only for pools whose base token is an RWA row with `underlyingTicker` and a US-equity `marketStatus`. For every other pool they are `available: false, reason: "not_us_equity"`.

The session clock is the one already used in the execution plane (`isUsEquityOpen` in `D:\4lpha-execution\src\trade\universe.ts`: `America/New_York`, Mon–Fri 09:30–16:00, no holiday calendar). Port it here as a pure function; add a NYSE holiday list only if cheap and documented, otherwise state "no holiday calendar" in the metric's `reason` vocabulary. Bucket convention stays UTC-open-ms; the session boundaries are converted to UTC per day.

| Metric | Definition | Inputs | Unit |
|---|---|---|---|
| `sessionState` | `rth` / `close` (16:00–20:00 ET) / `overnight` (all else, incl. weekends) at `calculatedAt`; plus `nextBoundaryAt` | clock | enum + ms |
| `lastRthClose` | close of the last bar whose bucket ends at or before the most recent 16:00 ET, with `asOf` | bars + clock | price unit |
| `gapPct` | (latest close − lastRthClose) / lastRthClose × 100; `reason: no_rth_close_in_window` when the 120-bucket window holds no RTH close | above | percent |
| `vwapSession` | Σ(typical price × volume) / Σ volume over bars since the current session start (RTH start for `rth`, 16:00 ET for `close`, previous 20:00 ET for `overnight`); `reason: unknown_volume_unit` / `invalid_volume` reuse RVOL's rules; `zero_volume` when Σ volume == 0 | bars + clock + volume | price unit |
| `vwapDistancePct` | (close − vwapSession) / vwapSession × 100 | above | percent |
| `orbHigh` / `orbLow` | high / low over the bars whose buckets fall inside 09:30–10:00 ET of the current RTH day; on 15m that is two bars, on 1h it is unavailable (`reason: interval_too_coarse`); before 10:00 ET or outside RTH `reason: orb_not_formed` / `not_rth` | bars + clock | price unit |
| `orbBreakPct` | (close − orbHigh) / orbHigh × 100 when close > orbHigh, (close − orbLow) / orbLow × 100 when close < orbLow, else 0 with `available: true` | above | percent |

Note the pool trades 24/7 while the reference does not: `gapPct` measures the on-chain price against the on-chain last-RTH close, not against Binance's NAV. The NAV-vs-pool relation is already `premiumBps`; do not conflate the two.

### 3.3 Equity regime fact (new small route, not a per-pool metric)

`GET /trading/regime/us-equity` (auth, envelope, staleness as everywhere). One record:

```
{ asOf, sessionState, nextBoundaryAt,
  spy: { pool, interval: "1h", emaSpreadPct, roc10Pct, rsi14, macdHistogram, gapPct, available },
  qqq: { ...same },
  regime: "risk_on" | "risk_off" | "neutral" | "unavailable",
  reasons: string[] }
```

Computed from the SPYB and QQQB pools' 1h rev 2 features that the producer already refreshes. Rule (deterministic, documented, replayable): `risk_off` when both ETFs have `emaSpreadPct < 0` and `roc10Pct < 0` on 1h, or either has `gapPct ≤ −2.5`; `risk_on` when both have `emaSpreadPct > 0` and `roc10Pct > 0`; else `neutral`. `unavailable` when either ETF's 1h evidence is missing or stale. The execution plane will blend this with CoinMarketCap's crypto-wide `get_global_metrics_latest` (that one is a whole-crypto snapshot and only a weak proxy for equities; this route is the equity leg).

If SPYB/QQQB are not in the feature pool set today, add them to the watch set with the same $10k depth floor recorded in `TRADFI-DATA-RESULT-2026-09-17.md`; do not special-case them past the floor.

## 4. Contract additions

- `parameters.indicatorRevision: 2`, `indicatorWarmupBars` extended with `{ bb20: 20, stochRsi14: 42, vwapSession: 1, gap: 1, orb: 2 }`, plus `sessionClock: { timeZone: "America/New_York", rth: "09:30-16:00", close: "16:00-20:00", holidays: "none" | "<list version>" }`.
- Input snapshot (`/input`) gains `referenceSession: { underlyingTicker, marketStatus, openState, asOf }` so replay of the session metrics is exact; bars themselves are unchanged.
- Rev 1 consumers must keep decoding: the execution plane's `decodeFeature` (`D:\4lpha-execution\src\trade\features.ts`) reads `parameters.indicatorRevision === 1` and the five additive metrics; rev 2 must keep those five byte-identical and add the new ones beside them. The execution plane will bump its decoder to accept `1 | 2`.
- `/pools` index: mark each pool with `usEquity: boolean` so consumers know which pools carry §3.2.

## 5. Tests the build must ship

- Fixture bars with a known SMA/σ → BB values by hand; zero-width band → `zero_width`.
- StochRSI at exactly 42 bars available, 41 bars → unavailable with `requiredBars: 42`.
- Session clock: a Friday 15:59 ET bar → `rth`; 16:00 → `close`; Saturday → `overnight`; DST switch week (March/November) → correct UTC boundaries.
- Gap: window with an RTH close → value; weekend-only window → `no_rth_close_in_window`.
- VWAP: volume unavailable → same reason vocabulary as RVOL; Σ volume 0 → `zero_volume`.
- ORB on 15m: two 09:30/09:45 bars → high/low; 1h → `interval_too_coarse`; 09:50 ET → `orb_not_formed`.
- Regime: four cases (risk_on, risk_off by EMA/ROC, risk_off by gap, unavailable) on fixed fixtures.
- Replay: a rev 2 snapshot recomputes to the same `snapshotId` after JSONB key reordering (the existing sorted-hash fixture pattern).
- Rev 1 regression: an existing rev 1 fixture produces byte-identical rev 1 fields under rev 2.

`node --test` offline only; quote TESTS / PASS / SKIPPED.

## 6. Out of scope here

No BUY/SELL logic, no thresholds, no weights (those are the execution plane's score spec); no paid CoinMarketCap calls (the execution plane pays and owns that budget); no changes to chart routes; no holiday-calendar purchase.

## 7. Report back

When done, append a short "implemented" note at the top of this file (like `TRADING-FEATURES-HANDOFF-2026-09-06.md`), list the route/field additions, and leave the execution plane a one-paragraph pointer in `D:\4lpha-execution\.agents\HANDOFF.md`.

## 8. Follow-up 2026-09-22 — coverage blocks the score: features for every pinned bStock pool

Observed on the first V3 cycles of a fresh local hire (`tradfi-trade-agent-01`, 12 routeable candidates): only 4 candidates carried evidence (`active` 1.00 / 0.53); 8 scored `insufficient-evidence` with `active = 0.09` (regime only) because `/trading/features/v2/pools` serves 8 pools (`marketplace_reference_pools`, `MAX_POOLS = 10`, `src/jobs/tradingFeatures.ts:15`) of which 4 are US-equity (two bStocks + SPYB + QQQB). Under the execution plane's 0.35 active-weight floor those 8 tokens can never be bought. The execution plane's `selectFeaturePools` already accepts up to 69 tokens, batched by 10 pools per request.

Ask: (1) raise `MAX_POOLS` (and the watchlist validator at line 70) to cover the whole TradFi pin, today 28 bStock/Ondo pools plus SPYB/QQQB — 32; (2) default the selection to "every pool the RWA allowlist admits at the $10k depth floor" so a new admission is covered automatically (the operator watchlist stays as an override); (3) keep the 10-pool batch bound on the read route so the client's paging is unchanged; (4) producer cadence: recompute each series at `close + publicationLag` rather than up to ~5 min later (observed 16:00 → 16:05:38 on SPYB on 2026-09-21), because readers reject the previous bar after `close + step + 90 s` and a cycle landing in that window scores nothing. Provider budget is the constraint to state: 32 pools × 2 intervals on the current OHLCV source.

## 9. Follow-up 2026-09-22 (afternoon) — measured after the 36-pool deploy

Execution side: the worker's index reader rejected any index over 10 pools, so the wider index produced ZERO evidence until `690fe71` (bound now 69, batches of 10). After that fix, read from the live plane at ~13:30 +07 (overnight session): 28 pinned tokens → 17 with evidence (5 full, 12 partial), 11 none.

Data-plane side, from `/trading/features/v2/pools` `series[].producer` on 15m + 1h (72 series):
- **42 `unavailable: admission_limit` (`cache:admission_limit`)** — the producer admits only part of the pool set per refresh and the rest queue indefinitely; this is now the single biggest gap. Please raise the per-cycle admission (or spread the refresh across the interval) so all 36 × 2 series are computed at least once per bar.
- 7 `unavailable: stale` / `stale_input` beyond the admission ones — same root: never refreshed in time.
- 16 `partial: gap` (1–23 missing bars in the 120 window on thin bStock pools, both Gecko and DexPaprika agree) — expected off-hours; nothing to do unless a filled-bar policy is wanted for illiquid pools (state it explicitly if so; the execution plane treats gapped metrics as unknown).
- `interval_too_coarse` (1h ORB) and `not_rth` are by design.

## 10. Follow-up 2026-09-22 03:37Z — after the §9 fix: the bottleneck moved to provider budget

Measured on the live plane (36 pools, 72 series on 15m + 1h): `admission_limit` is gone, but 18 series are `budget_exhausted` and 17 × 15m are `stale_input`; the source rows say `geckoterminal:budget_exhausted` (some `rate_limited`) AND `dexpaprika:budget_exhausted`, `consecutiveFailures` 2–3, retried every ~2 min. So the producer now admits everything and immediately runs out of OHLCV budget; 15m bars expire before their next successful refresh. Execution-side coverage went from 17/28 (5 full) at 03:21Z to 21/28 but only **2 full** at 03:37Z.

Ask: (1) pace the 15m refresh to the provider budget — spread the 36 pools across the 15-minute bar instead of bursting at the close, and prefer 15m over 5m (the execution plane never reads 5m; 36 × 5m series are the budget's biggest consumer); (2) treat `rate_limited` as a back-off, not a failure count; (3) publish the remaining budget per provider on `/pools` `meta.producer` so the next check can tell "exhausted" from "paced". Until then a cycle sees roughly 2–5 fully scored tokens.

Implemented (§10 follow-up, 2026-09-22): built and verified offline — `npm run typecheck` and `npm run build` clean, `node --test` **TESTS 662 / PASS 662 / FAIL 0 / SKIPPED 0**. Not committed, not deployed.

- **Binance Web3 checked and rejected as a substitute (operator asked, in-session):** `fetchSintralKlines` (`src/adapters/binanceWeb3.ts`) takes a *token* address, not a pool address — it is the same token-aggregate kline product that already backs `/klines/:address` (OnchainOS → Sintral), architecturally distinct from the exact-pool `/pools/:address/ohlcv` chain (GeckoTerminal → DexPaprika) this producer is built on. Swapping it in would blend prices across every venue a token trades on, breaking the single-pool identity this whole lane pins to (`defaultRwaFeatureSelection`'s deepest-venue choice, §8; QQQB's Uniswap v3 pool being larger than its PancakeSwap one is exactly the case this would silently mix in) and the replay/snapshot identity guarantee (`poolAddress`/`baseAddress`/`quoteAddress` recorded exactly). The adapter's own comment also flags "bapi endpoints throttle aggressively per source IP," so it is not obviously headroom-free either. Not implemented; flagged as a real architecture decision (accept token-aggregate pricing for the RWA lane) rather than an adapter swap, for the operator to decide separately if still wanted.
- (1a, "prefer 15m over 5m"): new `INTERVAL_PRIORITY` (`src/jobs/tradingFeatures.ts`) sorts `1h`/`15m` (tied, unchanged relative order) ahead of `5m` in the due-candidate queue, before the existing age-based fairness tiebreak. `5m` is deprioritized, not removed — it still gets whatever capacity is left over. Does not reorder `1h` ahead of `15m`; the top-of-hour collision between those two (flagged as a known residual in §9) is unchanged and still open.
- (1b, "spread across the bar"): not built as literal request-pacing/staggering. The 90-second `close + step + grace` freshness window (`PUBLICATION_LAG_MS`/`SCHEDULING_GRACE_MS`, `src/query/tradingFeatures.ts`) means a pool refreshed early in its own bar is refreshing *stale* data, not pre-warming the bar that hasn't closed yet — spreading naive attempts across the 15 minutes doesn't get new information sooner, since bars are globally aligned (`bucket convention stays UTC-open-ms`, an explicit invariant) and every pool's bar closes at the same instant regardless of scheduling. (1a) reduces demand at that shared instant instead, which is the lever that's actually available without touching bucket alignment.
- (2): `rate_limited` added to `QUIET_REASONS` — a pass that is entirely real 429s is no longer reported as a job failure. It is deliberately **not** added to `SELF_THROTTLE_REASONS`: a 429 is a genuine answer from the provider (unlike `admission_limit`/`budget_exhausted`, which mean the attempt never left this process), so it still escalates that candidate's own backoff — new test pins both halves of this distinction.
- (3): `GET /trading/features/v1|v2/pools` now reports `meta.ohlcvTransport` (the existing `poolOhlcvDiagnostics`, already used by `/status`) — process-scoped `requests`/`failures`/`rateLimited`/`budgetDenied`/`cooldownDenied` per provider, without a second call. Named `ohlcvTransport` rather than the ask's literal `meta.producer`, since `series[].producer` already means something different (per-candidate `FeatureAttempt`) on this same response.
- **Honest gap, not claimed as fixed:** §10's own diagnosis ("real `rate_limited` (429) responses from GeckoTerminal were observed, not just our own self-imposed `budget_exhausted`") suggests the transport may be delivering its budget *bursty* — up to ~10-22 near-simultaneous requests once a minute rather than spread through it — which can trip a real rate limiter even when the per-minute average is within a number that looked safe on paper. (1a) reduces total demand, which should help, but this run did not add actual request-pacing/jitter inside a cycle to smooth burst delivery, since getting the stagger amount right against the existing 25s per-request / 30s job-level timeouts needs live measurement, not a guessed constant — flagging as the next thing to check if `rate_limited` is still frequent after this deploys.

**Correction + follow-through (2026-09-22, same day, operator directive: "tắt 5m, dàn 36 pool 15m đều trong 15 phút ≈2-3 req/phút, coi rate_limited là back-off"):** the (1b) note above was wrong about *why* spreading wouldn't help, and that error is worth stating plainly rather than quietly fixing. The read-side staleness cutoff is not "close + 90s" — it is `closeTime + step + PUBLICATION_LAG_MS + SCHEDULING_GRACE_MS`, i.e. **a full extra bar period plus 90s**: ~16.5 minutes of slack for a 15m series, not 90 seconds. §8's/§9's own prose already quoted the right formula ("close + step + 90s"); the mistake was treating that as an effectively-90-second deadline when computing `DUE_PER_CYCLE`, which is what pushed admission up to 22/cycle and produced the real 429s this section diagnosed. Corrected now, built and verified offline — `npm run typecheck` / `npm run build` clean, `node --test` **TESTS 663 / PASS 663 / FAIL 0 / SKIPPED 0**. Not committed, not deployed.

- **"Tắt 5m" (turn it off, not just deprioritize):** the marketplace default (`featureSelection`'s unconfigured path) now returns `intervals: ["15m", "1h"]` only — `5m` is never produced for the 36-pool default set at all. An explicit operator watchlist (`FEATURE_WATCHLIST_KEY` set) still gets all three intervals unchanged, since it may exist for a reason other than the TradFi score. `INTERVAL_PRIORITY` from the first §10 pass stays in place for that operator-watchlist case (and as a second line of defense generally) but no longer does anything for the default set, since `5m` is absent from it entirely now.
- **"Dàn đều ~2-3 req/phút, không burst":** `DUE_PER_CYCLE` (`src/jobs/tradingFeatures.ts`) lowered 22 → 5, using the corrected ~16.5-minute tolerance rather than the mistaken 90-second one. With `5m` gone, steady-state demand for the ~40-pool default is `40/15 (15m) + 40/60 (1h) ≈ 3.3/minute` — the existing age-fairness `due` sort, unchanged otherwise, naturally rotates a same-instant 36-pool close across roughly a dozen 60s cycles at this pace, comfortably inside the ~16.5-minute tolerance rather than trying to burst it into 1-2 cycles. `MAX_IN_FLIGHT_REFRESHES`/`BUDGETS` (`poolOhlcv.ts`/`ohlcvTransport.ts`) were left at their §9 values (22/10/12): they are now generous headroom rather than the active constraint, and lowering them further wasn't needed to hit the requested pacing, so it wasn't done.
- (2) `rate_limited` handling from the first §10 pass is unchanged (already correct against this same directive): still backs off per-candidate, no longer fails the whole job pass.
- Tests rescaled for the new cap (22 → 5): `test/tradingFeatures.test.ts`'s cap-fairness and interval-priority tests use smaller pool counts to keep demonstrating the same properties at the new scale; new test pins that the default selection's `intervals` excludes `5m` while an explicit watchlist's doesn't.

## 11. Follow-up 2026-09-22 04:30Z — Sintral (Binance Web3) klines as the US-equity source; measured

Neural Alpha's live agent reads its bars from `https://dquery.sintral.io/u-kline/v1/k-line/candles?address=&interval=15min&limit=48&platform=bsc` with only `clienttype: web` / `clientversion: 1.2.0` headers, no key (their `integrations/binance-web3-market.ts`). This repo already has the same adapter, `fetchSintralKlines` in `src/adapters/binanceWeb3.ts`, used by `/klines` but not by trading features.

Measured on INTCB `0xe614…5283` (a series Gecko/DexPaprika left `budget_exhausted`): HTTP 200 on `15min` and `1h`, 120 rows each; the 15m bar for 04:15Z was served at 04:28Z; 1h buckets aligned; **36 back-to-back requests in 3 s → 36/36 200, no 429**. Rows have SEVEN columns `[o,h,l,c,v,timestamp,tradeCount]` (the adapter reads six; fine). Caveat that matters for v2 validation: Sintral **omits buckets with no trades** — the 120 × 15m rows spanned 48.5 h with 75 missing buckets over the weekend/overnight; 1h had 11 missing. So Sintral fixes quota and freshness, not thin-pool gaps.

Ask: (1) add Sintral as the first source for `usEquity` pools on 15m and 1h (Gecko/DexPaprika stay as fallback for the rest), map `15m→15min`, keep the v2 identity/bucket validation; (2) the empty-bucket policy needs an operator ruling before build — either keep "gap = metric unavailable" (current; thin pools score nothing off-hours) or forward-fill the close with volume 0 for missing buckets and mark the bar `filled` so EMA/RSI/MACD run on the time axis while RVOL still reads low. Neural Alpha implicitly does the latter by computing on whatever bars exist; the execution plane's 0.35 floor makes the choice visible either way.

### §11 ruling (operator, 2026-09-22 ~11:45 +07) — build this

1. **Source order for `usEquity` pools, 15m and 1h: Sintral (Binance Web3 u-kline) → GeckoTerminal → DexPaprika.** Non-equity pools keep the current order. Map `15m → 15min`, `1h → 1h`; request `limit=120` (the v2 window). Keep the `clienttype: web` / `clientversion: 1.2.0` headers the existing adapter sends; no key. Gate Sintral behind the same 6-concurrent semaphore Neural Alpha uses (`KLINE_CONCURRENCY=6`) even though the probe saw no throttling — the producer's steady load is ~5 req/min, the probe was 12 req/s.
2. **Empty-bucket policy = forward-fill** (ruled over trade-count-axis and over keep-gap). For every missing bucket inside the 120-bucket window: `open = high = low = close = previous real close`, `volume = 0`, bar flagged `filled: true`. Filled bars feed EMA/RSI/MACD/ATR/momentum/BB/StochRSI on the TIME axis; RVOL and the volume baseline use real volumes only (a filled bar contributes 0, so RVOL honestly reads low off-hours). Never fill BEFORE the first real bar in the window; never fill the current (unclosed) bucket.
3. **Real-bar floor:** a series with fewer than **30 real bars** in the 120-bucket window is `unavailable: too_few_real_bars` (a dead pool must not produce flat-but-valid indicators). Publish `coverage.realBars` and `coverage.filledBars` beside `contiguousBars`.
4. **Provenance:** `identity.source` = `sintral` when it served the bars; `input` snapshot keeps the raw Sintral rows (7 columns incl. `tradeCount`) so replay reproduces the fill deterministically; the fill is part of `calculateFeatures`, versioned as `indicatorRevision: 3` (rev 2 numbers unchanged for unfilled series). The execution plane accepts rev 2 and 3 alike and reads `filled` only for display.
5. Success check: after deploy, `/trading/features/v2/pools` should show every `usEquity` 15m/1h series `partial`/`fresh` within one bar of close, with no `budget_exhausted`/`stale_input`; the execution-side coverage probe (`D:\4lpha-execution\scripts\tmp\v3-coverage.mts`) should show ≥ 20 of 28 pinned tokens `active ≥ 0.78` off-hours.

Why: Neural Alpha computes on Sintral's trade-count axis with no fill and 50 synthetic seed bars (`data/market.ts`); that makes an "EMA12" mean 3 hours off-hours and 3 hours-of-RTH in session, and every bar carries volume. Our score weights volume/ATR and our exit triggers watch 1h MACD/EMA, so the time axis must be real; forward-fill keeps it real while opening off-hours coverage.

Implemented (§11, 2026-09-22): built and verified offline — `npm run typecheck` and `npm run build` clean, `node --test` **TESTS 675 / PASS 675 / FAIL 0 / SKIPPED 0**. Not committed, not deployed, and **no live probe against the real Sintral endpoint was run from this session** — only the offline fixtures below.

- **`indicatorRevision: 3` lives inside the existing `pool-features-v2` payload, not a new `FeatureVersion`/route.** Re-reading the ruling: "the fill is part of `calculateFeatures`, versioned as `indicatorRevision: 3`" is the same pattern as rev 1→2 — one additive parameter value inside `FEATURE_VERSION_V2`, not a new `/trading/features/v3` prefix or storage key. `calculateFeatures` now computes `indicatorRevision = input.source === "sintral" ? 3 : 2` whenever it runs the `FEATURE_VERSION_V2` branch; rev 1 is completely untouched (no fill, no `too_few_real_bars`, `indicatorRevision` stays absent) regardless of source. `src/query/tradingFeatures.ts`.
- **Forward-fill** (`calculateFeatures`, gated on `indicatorRevision === 3` only): the raw (possibly gapped) bars are built exactly as before, then — only for a Sintral-sourced rev 2 computation — walked from the first real bar to `cutoff` (the same closed-bar boundary the raw-bar loop already uses) filling any missing bucket with `open=high=low=close=previous real close, volume=0`. Every existing indicator function (`ema`, `rsiAt`, `macd`, `bb`, `stochRsi`, `atr`, `vwap`, `orb`…) reads this working series unchanged — the fill is one substitution of `bars` before any of them run, not a rewrite of each one. `coverage.realBars`/`coverage.filledBars` are new, always-present fields (0 filled outside a Sintral gap-fill, so old fixtures are untouched). `<30` real bars is a new `globalReason` branch (`too_few_real_bars`), same precedence tier as `invalid_identity`/`stale_input`, so it cascades to every metric including the session ones. `"sintral"` added to the identity source allowlist (both rev 1 and rev 2 now accept it). Six new tests in `test/tradingFeatures.rev3.test.ts` pin: fill-enables-availability, exact carried-forward value (not an average or zero), never-fill-before-first-real, the 30-bar floor even with plenty of filled bars, non-Sintral/rev-1 sources staying unfilled, and the identity allowlist.
- **Source integration** (`src/query/poolOhlcv.ts`'s `refresh()`): `GetPoolOhlcvParams` gained `usEquity?: boolean` (a fetch-strategy hint, deliberately left out of the cache key — same series either way). For `usEquity && currency === "usd" && token` on `15m`/`1h` specifically, `sources` becomes `[sintral, geckoterminal, dexpaprika]`; every other case is unchanged. `src/jobs/tradingFeatures.ts` threads `c.usEquity` (already computed per candidate, `references.has(tokenAddress)`) into the `getPoolOhlcv` call.
- **Currency: an interpretation, not explicit in the ruling.** Sintral has no on-chain pair — it can only answer "this token's price in USD" (Binance's own reference), never a ratio against a specific quote token, so it cannot serve the `currency: "token"` requests the RWA/equity-leg default selection used before this. Read that as one consistent decision for the whole chain: `defaultRwaFeatureSelection` and the SPYB/QQQB equity legs now request `currency: "usd"` (was `"token"`), so Sintral *and* its Gecko/DexPaprika fallback all run in the same denomination for a given usEquity series, rather than switching currency depending on which source happened to answer. The 4 legacy major pools (WBNB/BTCB/ETH/majors, not usEquity) are untouched, still `"token"`.
- **Quote identity for a Sintral-served record: a second interpretation.** With no on-chain pair, there is no real "quote token" to report; `base` is the requested token, `quote` is the canonical BSC USDT address (`USD_ANCHOR`, `jobs/majorsPrices.ts` — the same USD-anchor token this codebase already treats as the de facto USD proxy elsewhere) as a nominal stand-in for display/provenance, not a claim that Sintral priced against that specific pair. **Known, accepted edge case:** if a pool's *real* on-chain quote differs from canonical USDT, the existing "pair identity changed" safety check (`refresh()`, prevents silently swapping a series' identity) can reject a same-cycle Gecko/DexPaprika fallback attempt after a Sintral-served record is cached, because their real quote won't match the nominal one. This self-heals (next successful Sintral fetch re-anchors the record) and was left as-is rather than weakening that check speculatively — CLAUDE.md's own measurements say most of these pools' deepest venues are already USDT-quoted, so it should be rare in practice, but it hasn't been verified live for all 36.
- **Concurrency gating** (`src/adapters/binanceWeb3.ts`): `fetchSintralKlines` now goes through the same `withBinanceLimit` semaphore (6 concurrent) as every other Binance-host call in this file, and sends `clienttype: web` / `clientversion: 1.2.0` headers. Correction to the ruling's own wording: it says "keep… the headers the existing adapter sends," but this adapter sent no custom headers at all before this change (checked directly) — read as "add them," matching Neural Alpha's own working request, not "leave alone."
- `limit: 120` for the Sintral fetch specifically (not this file's general `HISTORY = 500`), matching the ruling.
- **Not done, stated rather than silently skipped:** the "empty" (zero-real-bars) and malformed-row cases fall through the existing generic `data.candles.length === 0` / catch-block handling already used for Gecko/DexPaprika — no Sintral-specific reason strings were added beyond what the shared error-categorization regex already produces (`rate_limited`/`cooldown`/`budget_exhausted`/`invalid_candles`/`identity_mismatch`/`provider_error`), since none of those needed changing for this source. `fetchSintralKlines` is deliberately *not* routed through `OhlcvTransport`'s per-minute budget/cooldown (the Gecko/DexPaprika mechanism) — only ruled to share `withBinanceLimit`'s concurrency cap, consistent with how the rest of `binanceWeb3.ts` is already protected.

## 12. Follow-up 2026-09-22 05:32Z — after rev 3 / Sintral / forward-fill

Works: `budget_exhausted` is gone; rev 3 + `source: sintral` + `currency: usd` decode on the execution side (`0c32b90`); the agent's 12:30 +07 cycle scored **12/12 candidates with full evidence** (all `below-threshold`, none `insufficient-evidence`) — the first such cycle.

Remaining, measured at 05:32Z: most 15m series show `lastSuccessAt 05:05–05:16` and `nextAttempt 05:30–05:34`, i.e. the 15m refresh runs ~15–19 min after a bar closes, so between the bar's expiry (`close + 15 m + 90 s`) and the next recompute every reader sees `stale_input` (execution coverage swung from 15/28 full at 05:10 to 9/28 at 05:32 with the same data). Please schedule the 15m recompute at `close + publicationLag` (1–2 min), the same way the 1h series is timed. Four pools (`0xf69e…`, `0x16cd…`, `0xeee9…`, `0xdabb…`) never succeed on any source: Sintral returns 1–7 contiguous bars (no trades); they are correctly `unavailable` under the 30-real-bar floor and should be reported as such rather than retried every minute.

Implemented (§12, 2026-09-22): built and verified offline — `npm run typecheck` and `npm run build` clean, `node --test` **TESTS 676 / PASS 676 / FAIL 0 / SKIPPED 0**. Not committed, not deployed, no live re-probe from this session.

- **Root cause, traced rather than assumed:** the 15-19 min lag is not the `close + publicationLag` formula (`refreshAfter`, unchanged since §8/§9 and already correct) — it's that a full round-robin rotation through ~40 same-instant 15m candidates at `DUE_PER_CYCLE = 5` (§10's follow-up) simply takes that long, `40/5 = 8` cycles minimum, worse in practice with any contention from `1h`/majors. §10's `DUE_PER_CYCLE = 5` was sized correctly *for the world before §11* — Gecko/DexPaprika-only, where the ceiling worth respecting was their combined budget (22/min). §11 moved most of these series onto Sintral first, whose own real ceiling (measured: 36 requests in 3s, no throttling) is far above that. `DUE_PER_CYCLE` raised 5 → 40 (`src/jobs/tradingFeatures.ts`) and `MAX_IN_FLIGHT_REFRESHES` 22 → 40 alongside it (`src/query/poolOhlcv.ts`, kept 1:1 per the §9/§10/§12 precedent) so a same-instant 15m close can clear in about one cycle when Sintral is healthy. A Sintral outage falling everything back to Gecko/DexPaprika at once is not a new risk this creates: `budget_exhausted` is already a `SELF_THROTTLE_REASONS` case (§9/§10 — flat retry, no backoff escalation), so a burst against that budget degrades to roughly the old §10 pace rather than failing hard.
- **`15m` now strictly outranks `1h`** (`INTERVAL_PRIORITY`, was tied): tied, `1h` — refreshed a quarter as often — almost always won ties under the age-fairness sort, exactly backwards, since `1h`'s own ~92-minute tolerance can absorb a few cycles' delay for free while `15m`'s ~16.5-minute one cannot. This is the direct fix for "recompute 15m the way 1h is [effectively already] timed." `5m` (default-selection pools don't have it at all since §10; an operator watchlist still can) stays last.
- **The "retried every minute" four-pool case, traced to a specific mechanism, not just observed:** a `too_few_real_bars` result is a *success* (`updated++`, no backoff), so its `nextAttempt` used the same `Math.max(now() + 60_000, snapshot.refreshAfter)` as any other success. For a persistently thin/dead pool, `refreshAfter` (computed from its own stale `closeTime`) is often already in the past by the time it's finally computed, so the `Math.max` just picks the 60-second floor — every single cycle, forever, for an outcome unlikely to change soon. `runTradingFeatures` now uses a 5-minute floor specifically when the computed reason is `too_few_real_bars` (still `updated++`/`consecutiveFailures: 0` — this is not a failure), freeing that admission slot for the ~40 other series that actually need it. New test pins this against the ordinary 60s floor for every other outcome.
- Tests rescaled for the new constants: `test/tradingFeatures.test.ts`'s cap-fairness test now needs 27 pools (2×40 < 81) to still demonstrate capping, and its interval-priority test now demonstrates `15m` strictly beating `1h`, not a tie against `5m` alone. `test/poolOhlcvFallback.test.ts`'s in-flight-cap test needed a real redesign, not just a bigger number: at `MAX_IN_FLIGHT_REFRESHES = 40`, above the real Gecko+DexPaprika budget (22) and unable to use a blocked-fetch pattern against Sintral either (`withBinanceLimit` is a true wait-for-a-slot semaphore — a fetch blocked *inside* it stalls every later waiter forever, unlike a budget check that fails fast) — it now blocks `store.acquireSchedulerLease` (before any source is even chosen) instead of the fetch itself, isolating the in-flight cap from every real provider's own ceiling.
